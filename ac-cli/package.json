{
  "name": "ac-cli",
  "version": "1.0.0",
  "description": "Copy agents.md and skills to Claude, Codex, Amp, and OpenCode projects",
  "type": "module",
  "bin": {
    "ac": "./dist/ac"
  },
  "scripts": {
    "build": "mkdir -p dist/bundled && cp -r ../skills/* dist/bundled/ && cp ../AGENTS.md dist/ && cp ../AGENTS-FULL.md dist/ && bun build src/index.ts --outfile dist/ac --target node",
    "dev": "bun run src/index.ts",
    "install:global": "bun install --global"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "inquirer": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/inquirer": "^9.0.8",
    "@types/fs-extra": "^11.0.4",
    "typescript": "^5.6.0"
  },
  "keywords": [
    "ai",
    "agents",
    "claude",
    "codex",
    "amp",
    "opencode",
    "skills",
    "cli"
  ],
  "license": "MIT"
}
